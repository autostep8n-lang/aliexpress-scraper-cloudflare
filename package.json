{
  "name": "aliexpress-scraper-cloudflare",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker foundation for a product intelligence and multi-platform scraping platform.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260817.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  }
}
